const defaults = {};
  
  /**
   * Field
   * @param {object} options -
   */
  const Field = (options) => {
    options = Object.assign({}, defaults, options);
    return {};
  };
  
  export { Field }